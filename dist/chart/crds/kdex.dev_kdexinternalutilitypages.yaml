---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kdexinternalutilitypages.kdex.dev
spec:
  group: kdex.dev
  names:
    categories:
    - kdex-i
    kind: KDexInternalUtilityPage
    listKind: KDexInternalUtilityPageList
    plural: kdexinternalutilitypages
    shortNames:
    - kdex-i-up
    singular: kdexinternalutilitypage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The state of the Ready condition
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.generation
      name: Gen
      priority: 1
      type: string
    - jsonPath: .status.attributes
      name: Status Attributes
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KDexInternalUtilityPage is the Schema for the kdexinternalutilitypages API

          A KDexInternalUtilityPage is an internal resource used to instantiate access to a utility page for a specific host.
          It is created by the KDex Host controller based on either a specific KDexUtilityPage/KDexClusterUtilityPage reference
          or a default system-wide utility page.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KDexInternalUtilityPage
            properties:
              contentEntries:
                description: contentEntries is a set of content entries to bind to
                  this page. They may be either raw HTML fragments or KDexApp references.
                items:
                  properties:
                    appRef:
                      description: appRef is a reference to the KDexApp to include
                        in this binding.
                      properties:
                        kind:
                          description: Kind is the type of resource being referenced
                          type: string
                        name:
                          description: Name of the referent.
                          type: string
                        namespace:
                          description: |-
                            Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                            namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                            referent.
                            If the referring resource is cluster scoped, this field is ignored.
                            Defaulted to nil.
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                      x-kubernetes-map-type: atomic
                      x-kubernetes-validations:
                      - message: '''kind'' must be either KDexApp or KDexClusterApp'
                        rule: self.kind == "KDexApp" || self.kind == "KDexClusterApp"
                    attributes:
                      additionalProperties:
                        type: string
                      description: attributes are key/value pairs that will be added
                        to the custom element as attributes when rendered.
                      maxProperties: 10
                      type: object
                    customElementName:
                      description: customElementName is the name of the KDexApp custom
                        element to render in the specified slot (if present in the
                        template).
                      type: string
                    rawHTML:
                      description: rawHTML is a raw HTML string to be rendered in
                        the specified slot (if present in the template).
                      type: string
                    slot:
                      description: slot is the unique name to which this entry will
                        be bound.
                      type: string
                  required:
                  - slot
                  type: object
                  x-kubernetes-validations:
                  - message: appRef must be accompanied by customElementName
                    rule: '!has(self.appRef) || self.customElementName != ""'
                  - message: exactly one of the fields in [appRef rawHTML] must be
                      set
                    rule: '[has(self.appRef),has(self.rawHTML)].filter(x,x==true).size()
                      == 1'
                maxItems: 8
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - slot
                x-kubernetes-list-type: map
                x-kubernetes-validations:
                - message: slot 'main' must be specified
                  rule: self.exists(x, x.slot == 'main')
              hostRef:
                description: hostRef is a reference to the KDexInternalHost that this
                  utility page belongs to.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: hostRef.name must not be empty
                  rule: self.name.size() > 0
              overrideFooterRef:
                description: overrideFooterRef is an optional reference to a KDexPageFooter
                  resource. If not specified, the footer from the archetype will be
                  used.
                properties:
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: '''kind'' must be either KDexPageFooter or KDexClusterPageFooter'
                  rule: self.kind == "KDexPageFooter" || self.kind == "KDexClusterPageFooter"
              overrideHeaderRef:
                description: overrideHeaderRef is an optional reference to a KDexPageHeader
                  resource. If not specified, the header from the archetype will be
                  used.
                properties:
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: '''kind'' must be either KDexPageHeader or KDexClusterPageHeader'
                  rule: self.kind == "KDexPageHeader" || self.kind == "KDexClusterPageHeader"
              overrideNavigationRefs:
                additionalProperties:
                  properties:
                    kind:
                      description: Kind is the type of resource being referenced
                      type: string
                    name:
                      description: Name of the referent.
                      type: string
                    namespace:
                      description: |-
                        Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                        namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                        referent.
                        If the referring resource is cluster scoped, this field is ignored.
                        Defaulted to nil.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                description: overrideNavigationRefs is an optional map of keyed navigation
                  object references. When not empty, the 'main' key must be specified.
                  These navigations will be merged with the navigations from the archetype.
                maxProperties: 10
                type: object
                x-kubernetes-validations:
                - message: '''main'' navigation must be specified if any navigations
                    are provided'
                  rule: size(self) == 0 || has(self.main)
                - message: all navigation kinds must be either KDexPageNavigation
                    or KDexClusterPageNavigation
                  rule: self.all(k, self[k].kind == 'KDexPageNavigation' || self[k].kind
                    == 'KDexClusterPageNavigation')
              pageArchetypeRef:
                description: pageArchetypeRef is a reference to the KDexPageArchetype
                  that this binding is for.
                properties:
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: pageArchetypeRef.name must not be empty
                  rule: self.name.size() > 0
                - message: '''kind'' must be either KDexPageArchetype or KDexClusterPageArchetype'
                  rule: self.kind == "KDexPageArchetype" || self.kind == "KDexClusterPageArchetype"
              scriptLibraryRef:
                description: scriptLibraryRef is an optional reference to a KDexScriptLibrary
                  resource.
                properties:
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: '''kind'' must be either KDexScriptLibrary or KDexClusterScriptLibrary'
                  rule: self.kind == "KDexScriptLibrary" || self.kind == "KDexClusterScriptLibrary"
              type:
                allOf:
                - enum:
                  - Announcement
                  - Error
                  - Login
                - enum:
                  - Announcement
                  - Error
                  - Login
                description: type indicates the purpose of this utility page.
                type: string
            required:
            - contentEntries
            - hostRef
            - pageArchetypeRef
            - type
            type: object
          status:
            description: status defines the observed state of KDexInternalUtilityPage
            properties:
              attributes:
                additionalProperties:
                  type: string
                description: attributes hold state of the resource as key/value pairs.
                maxProperties: 10
                type: object
              conditions:
                description: |-
                  conditions represent the current state of the resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Progressing": the resource is being created or updated
                  - "Ready": the resource is fully functional
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed for this resource. It corresponds to the
                  resource's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
